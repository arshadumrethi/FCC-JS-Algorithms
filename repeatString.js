function repeatStringNumTimes(str, num) {
    // repeat after me
    let result = "";
    for(let i = 0; i < num; i++) {
        result += str;
    } console.log(result);
  } 
  
  repeatStringNumTimes("abc", 3);